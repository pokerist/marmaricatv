PORT=5000
NODE_ENV=development
# Database configuration is handled through file paths
# Upload directory is relative to server directory
UPLOAD_DIR=uploads
# Set longer timeout for database operations if needed
DB_TIMEOUT=5000
# Log level for server output
LOG_LEVEL=info
